<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/new_store_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:text="Create a New Store"
        android:gravity="center"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="16dp"
        android:textColor="@color/black"
        android:textSize="26sp"
        android:textStyle="bold"/>

    <EditText
        android:id="@+id/new_store_name"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        app:layout_constraintTop_toBottomOf="@id/new_store_textview"
        android:layout_marginTop="36dp"
        android:textSize="16sp"
        android:layout_marginHorizontal="16dp"
        android:background="@drawable/background_edittext"
        android:paddingHorizontal="16dp"
        android:hint="Store Name"
        android:maxLines="1"
        android:inputType="text"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/create_store_button"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        app:layout_constraintTop_toBottomOf="@id/new_store_name"
        android:layout_marginTop="36dp"
        android:layout_marginHorizontal="24dp"
        app:elevation="12dp"
        android:enabled="false"
        android:background="@drawable/main_button_selector"
        android:textAllCaps="false"
        android:textSize="16sp"
        android:text="Create a Store"/>

</androidx.constraintlayout.widget.ConstraintLayout>