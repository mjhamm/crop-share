<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/bottom_sheet_background"
    android:paddingBottom="24dp">

    <EditText
        android:id="@+id/enter_street_address_edittext"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="24dp"
        android:textSize="16sp"
        android:inputType="textPostalAddress"
        android:maxLines="1"
        android:hint="Street Address"
        android:background="@drawable/background_edittext"
        android:layout_marginHorizontal="16dp"
        android:paddingHorizontal="16dp"/>

    <EditText
        android:id="@+id/enter_zip_code_edittext"
        app:layout_constraintTop_toBottomOf="@id/enter_street_address_edittext"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:textSize="16sp"
        android:inputType="number"
        android:maxLength="5"
        android:maxLines="1"
        android:hint="Zip Code"
        android:background="@drawable/background_edittext"
        android:layout_marginHorizontal="16dp"
        android:paddingHorizontal="16dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/save_store_location_button"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Update Store Location"
        app:layout_constraintTop_toBottomOf="@id/enter_zip_code_edittext"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="16dp"
        android:textAllCaps="false"
        android:textSize="16sp"
        app:cornerRadius="10dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>